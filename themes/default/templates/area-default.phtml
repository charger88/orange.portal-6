<?php

if ($blocks){
	echo $preHTML;
    /** @var OPAM_Block $block */
    foreach ($blocks as list($block,$block_data)){
		echo $this->fetch($block->get('content_template'), [
			'content'  => $block,
			'response' => $block_data,
		]);
	}
	echo $postHTML;
}